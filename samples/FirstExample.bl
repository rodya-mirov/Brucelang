define main() as {
    let x = 13 + 14;
    let y = 13 * (x - f(12));
    return x - g(y);
}

define f(arg) as {
    let helpful = (x => x*arg)(12);
    let moreHelpful = ((a, b) => (a+b)-arg)(34, 55);
    return helpful;
}

let curry = func => (x => func(x, 2, 3, 4));

define g(zed) as {
    define h(a, b, c, d) as {
        return a + b + f(c + d);
    }

    let curryPowder = curry(h);
    return curryPowder(f(1));
}
